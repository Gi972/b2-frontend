---
const tenant = Astro.locals.tenant;
---

<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <title>{tenant ? `Boutique ${tenant.slug}` : "BoutikOne – SSR test"}</title>
  </head>
  <body>
    <h1>{tenant ? `Boutique ${tenant.slug}` : "BoutikOne – SSR test"}</h1>
    {
      tenant ? (
        <div>
          <p>
            <strong>Compte Stripe:</strong> {tenant.stripe_account_id}
          </p>
          <p>
            <strong>Domaine:</strong> {tenant.domain}
          </p>
          <p>
            👍 Le multi‑tenant SSR fonctionne. Ajoute maintenant la liste
            produits.
          </p>
        </div>
      ) : (
        <div>
          <p>Aucun tenant détecté (middleware non exécuté ou host absent).</p>
        </div>
      )
    }
  </body>
</html>
